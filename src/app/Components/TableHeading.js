import React from "react";

import "../../scss/components/tableheading.css";

const TableHeading = ({ title }) => {
  return <div className="table-heading">{title}</div>;
};

export default TableHeading;
